// 레슨·특강 MDX에 주입하는 블록 라이브러리.
// slide-blocks와 분리 — slide-blocks는 슬라이드 전용 UI,
// lesson-blocks는 본문형 콘텐츠(터미널 mockup, 캡션 이미지 등)에 쓴다.
export { Terminal, type TerminalProps } from "./Terminal";
export { Figure, type FigureProps } from "./Figure";
